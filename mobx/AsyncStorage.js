import { AsyncStorage } from "react-native";

//const that keep track of username and instanceId
const USERNAME = 'USERNAME';
const INSTANCE_ID = 'INSTANCE_ID';

//export created const variables
export const asyncStorageKeys = {
  USERNAME,
  INSTANCE_ID
};